import {Container} from 'components/ui'

export default function Header() {
    return (
        <header className="bg-white shadow h-20 flex items-center">
            <Container className="text-red-500">
                hheader content from container
            </Container>
        </header>
    )
}
